import React,{Component} from 'react';
import AutoComplete from '../components/autoComplete';

const tabRecipe= (props)=>{
    return (<div className='row'>
            <h3>The autoComplete Component </h3>

            <AutoComplete />
        </div>);
}

export default tabRecipe;